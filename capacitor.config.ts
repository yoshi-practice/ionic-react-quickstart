import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yoshi1125hisa.ionic.starter',
  appName: 'ionic-react-sample',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
